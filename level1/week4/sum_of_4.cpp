#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n;
    cin >> n;

    unsigned long long ans = (2 * n + 1) * (2 * n + 1) * (2 * n + 1);

    cout << ans << endl;

    return 0;
}